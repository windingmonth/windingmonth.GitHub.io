apiVersion: v1
kind: Service
metadata:
  annotations: {}
  labels:
    app: firstchart
  name: firstchart
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: firstchart
  